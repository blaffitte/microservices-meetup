image: node:latest
cache:
  paths: 
    - node_modules/ 
before_script:
  - npm install -g grunt-cli
  - npm install
pages:
  stage: deploy
  script:
    - grunt default
  artifacts:
    paths:
      - public 